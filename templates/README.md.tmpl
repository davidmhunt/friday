# [SET AT SETUP: PROJECT_NAME]

[SET AT SETUP: one-paragraph project description — what this project does and why.]

---

## Overview

[SET AT SETUP: 1-3 paragraphs / a project-phases list describing the shape
and status of the work. This section is inherently project-specific — the
template can't pre-fill it, only hold its place.]

---

## Repository Layout

```
[SET AT SETUP: PROJECT_NAME lowercase]/
├── [SET AT SETUP: fill in this project's own top-level dirs]
├── docs/
│   └── research/                # Verified literature research memos (tracked, survives the harness)
├── .agents/ / .claude/          # Agent hooks, command guards, and subagent adapters
├── docker/                      # Docker build inputs (Dockerfile, compose files, entrypoint)
├── .friday/                     # Portable harness submodule
│   ├── templates/                 # Harness template sources (tracked, shared across projects)
│   └── active/harness/            # This project's generated harness state (gitignored inside the submodule)
│       ├── USER_GUIDE.md            # Comprehensive operator manual (start here!)
│       ├── harness.md                # Core loop rules and invariant definitions
│       ├── status.md                 # Living dashboard of active directives & background jobs
│       ├── status_history.md         # Append-only permanent ledger of completed directives
│       ├── plans/                    # Planner directives (next_steps.md, suggestions.md, goals.md)
│       ├── roles/                    # Role definition contracts (Planner, Controller, Reviewer, etc.)
│       ├── rules/                    # On-demand rule specification details
│       └── tools/                    # Reference ingestion, verification, and linting scripts
├── harness.sh                   # Wrapper for `.friday/setup/harness_sync.sh` (push/pull harness edits)
└── AGENTS.md                    # Agent root index loaded into AI sessions
```

---

## Multi-Agent Research Harness

Development and research in [SET AT SETUP: PROJECT_NAME] are orchestrated
via an autonomous multi-agent harness. The harness maintains continuity
across sessions, enforces rigorous verification gates, and tracks
provenance.

### Operator Workflow: Planner → Controller → Reviewer

Human operators interact with the repository using three primary roles:

1. **Planner (`you are the planner agent`)**: Triages open items from `.friday/active/harness/plans/suggestions.md`, creates structured directives in `.friday/active/harness/plans/next_steps.md`, sets verification commands (`Verify:`), and opens tracking issues (if configured).
2. **Controller (`you are the controller agent`)**: Autonomously executes open directives by dispatching and coordinating specialized subagents (`coder`, `runner`, `researcher`, `author`) without modifying files directly.
3. **Reviewer (`you are the reviewer agent`)**: Independently executes verification commands, validates citations, commits completed work (`Role: <description>`), closes tracking issues, and archives status to `.friday/active/harness/status_history.md`.

For complete details on operating the harness, monitoring jobs, and providing input files, see the [**Harness User Guide**](.friday/active/harness/USER_GUIDE.md).

---

## Quickstart & Environment

### Environment Setup

This project uses `[SET AT SETUP: PACKAGE_MANAGER]` for dependency/environment management.

```bash
[SET AT SETUP: PACKAGE_MANAGER_SYNC_CMD]     # sync dependencies
[SET AT SETUP: TEST_CMD]                     # run tests
[SET AT SETUP: PACKAGE_MANAGER_RUN_CMD] <script>   # run a script
```

<!-- SECTION:docker_quickstart:start -->
### Docker Dev Container

Docker builds in two stages: `dev` (base packages, LaTeX, the package
manager, [herdr](https://herdr.dev)'s terminal workspace manager — no agent
CLI) and `harness`, which layers Claude Code / Antigravity on top of `dev`.
This split means the container works two different ways depending on
whether you have the `.friday/` submodule checked out.

**With the submodule, after running `init_harness.py`:** setup wrote
`COMPOSE_FILE=docker/docker-compose.yml:docker/docker-compose.harness.yml`
into the gitignored root `.env`, so plain `docker compose` commands pick up
both files (the base `dev` service plus the harness override — agent config
volumes, `target: harness`) automatically:

```bash
docker compose build            # build the image
docker compose up -d            # start the container, detached
docker compose attach harness   # attach to the container's shell
# or, from another terminal (independent of the container's main process):
docker compose exec harness bash
docker compose down             # stop it (project dir untouched)
```

**Without the submodule** (e.g. a teammate who cloned without
`--recurse-submodules`, or never ran setup): there is no root `.env`, so
Compose falls back to just the base file and gets a working `dev` container
with no agent tooling on it. Point Compose at it explicitly with `-f`:

```bash
docker compose -f docker/docker-compose.yml up -d
```

Either way, the bind-mounted project directory is your host filesystem,
mounted at `/[SET AT SETUP: PROJECT_NAME_LOWER]`; named volumes keep auth
and caches across restarts. The image follows this project's own
`harness.config.env` (package manager, agent CLI, LaTeX, GPU) — after
re-running `init_harness.py` to reconfigure, run `build` again to pick up a
changed `docker/Dockerfile`. The container starts a plain shell; herdr is
installed unconditionally — run `herdr` by hand from that shell when you
want it. See `.friday/active/harness/USER_GUIDE.md` §12 for the full
workflow, including reconfiguring mounts.
<!-- SECTION:docker_quickstart:end -->
<!-- SECTION:latex_on:start -->
### LaTeX Document Compilation

Theory documents and presentation decks are individual self-contained LaTeX
projects built via `latexmk`:

```bash
# Build a docs/theory/ document
cd docs/theory/<content-slug> && latexmk -pdf main.tex

# Build a docs/report/ document or slide deck
cd docs/report/<content-slug> && latexmk -pdf main.tex
```
<!-- SECTION:latex_on:end -->

### Common Harness Commands

```bash
# Check markdown line-cap hygiene
python3 .claude/hooks/check_md_hygiene.py

# Verify bibliography links and local PDFs (if this project uses .friday/active/harness/tools/ bibliography workflow)
python3 .friday/active/harness/tools/verify_references.py

# Ingest new reference PDFs and BibTeX from docs/references/inbox/
python3 .friday/active/harness/tools/intake_references.py
```
