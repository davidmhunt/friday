# Next Steps — Sprint Backlog (Planner → Coder/Runner/Reviewer/Author/Researcher)

**Layer 4 of 4 — the sprint backlog.** The directives that break the current
sprint spec (`docs/specs/<slug>.md`) down into dispatchable work. Every
directive here **derives from a spec goal and names it** (`Serves: G2`), so
work traces up: directive → spec goal → epic (`long_term.md`) → project
objective (`goals.md`). A directive that serves no spec goal is either
scope creep or a missing spec amendment — say which, do not just add it.

Immediate directives from the Planner. Full directive text lives in
`plans/directives/<ID>.md` — one file per directive; **load only the
file(s) for your assigned directive**, not the whole directory. This file
holds the index, the current dispatch queue, and cross-cutting
riders/constraints. Pass narratives and rulings history: `plans/history.md`.
`plans/directives/<ID>.md` is gitignored (except `TEMPLATE.md`) and deleted
by the Reviewer at close-out — it's local working-session state, not the
durable record; that's `.friday/active/harness/status_history.md` (rule 3) and the
directive's tracker issue (rule 13, if a tracker is configured).

Update `coding/tasks_working.md` as you pick items up, and
`coding/tasks_finished.md` + a work record per rule 12 (its reference
pasted next to it) when done — see the `[DONE]` definition in
`harness.md`. Ping back via `plans/suggestions.md` if a directive is
ambiguous or you hit a blocker — do not silently reinterpret scope.
Also keep `.friday/active/harness/status.md`'s "Directive status" table current (rule 3) —
that's the live per-directive owner/state view; this table's queue rows are
the static spec, not the live state.

## Directive ID convention

`D<NN>`, one file per directive in `directives/<ID>.md`.

## Current sprint

**Spec:** _(none yet — the Architect writes one to `docs/specs/`, the user
signs it off, and the Planner opens the backlog from it. A spec still marked
`draft` is not dispatchable.)_

## Current queue

(none yet — opened by the Planner from the signed-off sprint spec)

<!-- Each row: ID | Title | Tier | Serves (spec goal) | Depends on | Tracker
     issue. The "Serves" column is what makes the sprint traceable; do not
     drop it. Budgets for prose deliverables come from the spec (rule 16)
     and are stated in the directive's Verify: block, not restated here. -->
