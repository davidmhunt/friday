# Finished Tasks

Closed items moved here by the Reviewer (`[DONE]`/`[REVIEWED]`), each with
its work-record reference pasted next to it (rule: `[DONE]` requirement 4 in
`.friday/active/harness/roles/reviewer.md`). Keep current + prior cycle in full; trim older
entries to a one-line `[ref] task — DONE` pointer (rule 8, markdown
hygiene).

(none yet)
