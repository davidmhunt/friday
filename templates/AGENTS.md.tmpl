# AGENTS.md

Guidance for coding agents in this repository. This file is loaded into every
session, so it stays lean — detail lives in on-demand docs. **Load only what
your task needs, per this index:**

> [!NOTE]
> `.friday/active/` lives inside the `.friday` git submodule and is
> gitignored *there* (it's generated, per-project state, not part of the
> friday repo's own history). A git-ignore-respecting search — `git grep`,
> or `rg` without a flag — will not find anything under
> `.friday/active/harness/`. Read those files directly by path, or pass
> `rg --no-ignore` if you need to search across them.

| Before you… | Read |
|-------------|------|
| Run any script or test | `.friday/active/harness/rules/environment.md` (env, launch pattern) |
| Launch/check/trust a long-running job or monitor | `.friday/active/harness/rules/monitoring.md` |
| Mutate any canonical data artifact (re-generate, re-render, ground truth) | `.friday/active/harness/rules/data_artifacts.md` — snapshot FIRST |
| [SET AT SETUP: add/remove rows for this project's own on-demand docs, e.g. an architecture doc, a GPU rule, a hardware-interface doc] | |
| Act as an assigned agent role | `.friday/active/harness/harness.md` (core rules) + `.friday/active/harness/roles/<your-role>.md` only |

## Project facts

| | |
|---|---|
| Project | [SET AT SETUP: PROJECT_NAME] — [SET AT SETUP: one-line project description] |
| Working root | `[SET AT SETUP: PROJECT_WORKING_ROOT]` |
| Results doc (rule 2) | [SET AT SETUP: where results/findings get recorded for this project] |
<!-- SECTION:latex_on:start -->
| LaTeX/Beamer drafting suite | `LATEX_DRAFTING_ENABLED=true` — Researcher drafts `docs/theory/`, Author builds `docs/report/` (`.friday/active/harness/roles/researcher.md`, `author.md`) |
<!-- SECTION:latex_on:end -->
| Environment | [SET AT SETUP: PACKAGE_MANAGER] (`[SET AT SETUP: PACKAGE_MANAGER_RUN_CMD] ...`); see `.friday/active/harness/rules/environment.md` |
| Accelerators | [SET AT SETUP: none / GPU details, or delete this row] |
| Work record (rule 12) | git + `[SET AT SETUP: VCS_REMOTE]`; a "record" = a commit, referenced by hash |
| Who records work | Reviewer, at close-out (never each role individually) |
| Queue mirror (rule 13) | [SET AT SETUP: TRACKER_KIND] — Planner opens, Reviewer closes (or "none" if not tracked externally) |
| Docker dev container | `[SET AT SETUP: DOCKER_ENABLED]` — when enabled, the image installs `[SET AT SETUP: PACKAGE_MANAGER]` plus the `[SET AT SETUP: ADAPTERS_ENABLED]` CLI(s); see `.friday/active/harness/USER_GUIDE.md` §12 to set up or reconfigure |

## Project Overview

[SET AT SETUP: 1-2 paragraphs describing what this project is and does —
this section is inherently project-specific prose, not something the
template can pre-fill. Cover: the problem domain, the shape of the repo
(what lives where at a high level), and any defining technical constraint
(e.g. accelerator target, external system it integrates with).]

## Session Continuity

Multiple agents/sessions work here, sometimes concurrently — treat state as
shared, not private to one session.

- **`.friday/active/harness/status.md`** is the living dashboard: per-directive status for
  OPEN directives only (owner, state, what's left — the answer to "what is
  directive X doing right now"), what's running, recent milestones, open
  issues. Read it at session start; update it on real progress, directive
  pickup/handoff, job start/finish, or blockers — it must reflect current
  reality. Keep entries terse; link to your results doc or the relevant
  script/output instead of duplicating detail. `.friday/active/harness/status_history.md`
  holds the same directive's record once the Reviewer closes it (rows move
  there, not deleted) — check it for the outcome of past directives.
  `.friday/active/harness/plans/directives/<ID>.md` is gitignored (except `TEMPLATE.md`)
  and deleted by the Reviewer at close-out — it's the working spec while a
  directive is open, not the durable record.
- **[SET AT SETUP: this project's results doc]** is the comprehensive
  results reference (see harness rule 2 — single source of truth for
  numbers). Update on significant milestones (new version, finalized
  result, systemic bug fix, real ablation) — not every incremental step.
  Prefer the pattern: result table + honest interpretation, including
  negative results with real caveats. [SET AT SETUP: if this project has
  separate theory/report namespaces (Researcher/Author), describe them
  here; otherwise delete this sentence.]

## Repository Layout

| Dir | Contents |
|-----|----------|
| [SET AT SETUP: fill in this project's own top-level dirs and what lives in each — e.g. `src/`, `notebooks/`, `docs/`, `data/`] | |
| `.friday/active/harness/` | Multi-agent harness: core rules, role definitions, per-rule detail docs, the live dashboard (`status.md` + `status_history.md`), and the role working folders (`plans/`, `coding/`, `running/`, `review/`). |
| `docs/research/` | The Researcher's memos — tracked project content, not part of the `.friday/` submodule's generated output. |

Conventions: all paths relative to the repo root
(`[SET AT SETUP: PROJECT_WORKING_ROOT]`), no hardcoded absolute paths in
code. [SET AT SETUP: PACKAGE_MANAGER] manages dependencies — every run goes
through `[SET AT SETUP: PACKAGE_MANAGER_RUN_CMD]`.

## Multi-Agent Workflow

Development uses seven roles — Controller, Planner, Coder, Runner, Reviewer,
Author, Researcher — assigned by the user at chat start ("you are the
planner agent"). If told to "run the coder and runner in this session," act
as Controller and spawn the roles as subagents (spawn titles `role(model):
task`, e.g. `coder(<mid-tier model>): <task>`; utility spawns keep plain
titles).

**CRITICAL:** `.friday/active/harness/harness.md` holds the loop and the shared rules every
role must follow; `.friday/active/harness/roles/<role>.md` holds your role's namespace,
tier, constraints, and handoff protocol. Read the core plus your own role
file — not the other roles' files. Rule detail docs under `.friday/active/harness/rules/`
are read only when a rule's trigger matches your next action.

## Command Execution & Approval Policy

Command execution is programmatically guarded by `.agents/hooks.json` via
`.agents/hooks/command_guard.py` before `run_command` executes:

- **Auto-allowed (`allow`)**: `[SET AT SETUP: PACKAGE_MANAGER_SYNC_CMD]`,
  `[SET AT SETUP: PACKAGE_MANAGER_RUN_CMD]`, `[SET AT SETUP: TEST_CMD]`,
<!-- SECTION:latex_on:start -->
  `latexmk`/`bibtex`/`pdflatex`/`xelatex`/`lualatex`,
<!-- SECTION:latex_on:end -->
  read-only git queries
  (`git status`, `git diff`, `git log`, `git show`, `git branch`),
  and non-destructive inspections (`ls`, `cat`, `grep`, `rg`, `find`, `pwd`).
- **Requires user approval (`force_ask`)**: State-modifying git commands (`git commit`,
  `git push`, `git checkout`, `git switch`, `git reset`), background jobs (`systemd-run`,
  `setsid`), dependency modifications (`[SET AT SETUP: PACKAGE_MANAGER_ADD_CMD]` and its
  remove counterpart), file deletions (`rm`, `unlink`), and unclassified commands.
- **Strictly forbidden (`deny`)**: Privilege escalation (`sudo`, `su`), force pushes
  (`git push --force`, and the `git push origin +main` refspec spelling), adding a new
  git remote, unscoped destructive wipes (`rm -rf` aimed at `/`, `~`, `.`, `..`, or a bare
  `*`), and fetching remote scripts into a shell (`curl | sh`, `curl … && sh …`,
  `eval "$(curl …)"`).

**Inside the Docker dev container this policy is reduced to its deny list.**
`docker-compose.yml` sets `ANTIGRAVITY_CONTAINER=1`, which switches the guard into
container mode: deny still applies, but force-ask and the allow list are skipped and
anything unrecognized runs without confirmation. Do not read a successful command in
the container as evidence that it would have been permitted on the host.

