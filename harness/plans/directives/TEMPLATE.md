# [TEMPLATE — not a real directive; copy this file to `<ID>.md` and delete
# this header when opening a real one]

# D01: <short, specific title — what does this directive produce>

**Opened:** <pass/date>, Planner<, from <source — user feedback, a
suggestions.md item, etc.>>. Tier `[light]` or `[heavy]`. Queue item:
<external item ref, if the project mirrors its queue (rule 13)>.

<1-2 sentences of context: why this directive exists, what decision or
request it traces back to. Directives in this project tend to accrete a lot
of ruling/amendment history as they run (open questions get answered,
scope gets amended) — that's normal; keep the amendments dated and in this
file rather than scattered across chat.>

## Scope

<What's in scope, what's explicitly out of scope. Be concrete — vague scope
is the #1 cause of a Coder/Runner silently reinterpreting a directive.>

## Plan / phases

<Break the work into phases if it's more than one sitting. Each phase
should be independently checkable.>

- **Phase 1 [light/heavy] — <name>:** <what gets built/run>.
  Verify: <the actual command or judgment criterion that proves this phase
  is done>.
- **Phase 2 …**

## Open questions

<Anything genuinely blocked on a user decision — list it explicitly rather
than guessing. Close each out with a dated note once answered.>

## Verify (directive-level)

<The overall bar for closing this whole directive — usually the union of
the phase-level Verify lines plus any cross-cutting check (e.g. "every
number in the output artifact cites the CSV it came from").>
