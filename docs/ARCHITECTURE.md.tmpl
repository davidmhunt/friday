# Architecture

Not yet defined — [SET AT SETUP: this project's main source directory, e.g.
`src/`] is currently empty. This doc will describe the core pipeline/model
architecture once real structure exists: major components, data formats,
and any versioning scheme for saved state.

Whoever writes the first real pipeline code (Coder, gated by a Planner
directive) owns filling this in — don't let the source tree accumulate
structure without this doc describing it.
